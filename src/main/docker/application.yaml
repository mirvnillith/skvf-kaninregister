server:
  port: ${PORT:8080}
skvf:
  google:
    credentials: "${GOOGLE_CREDENTIALS}"
    folder: "${GOOGLE_FOLDER}"
  addo:
    url: "${ADDO_URL}"
    email: "${ADDO_EMAIL}"
    password: "${ADDO_PASSWORD}"
  approval:
    url: "${APPROVAL_URL}"
  variant: "${VARIANT:}"
  feedback: "${FEEDBACK:kaninregistret@skvf.se}"
  